// Declare all upload folders here
var app = angular.module('bw', ['thatisuday.dropzone']).config(function($sceDelegateProvider) {
    $sceDelegateProvider.resourceUrlWhitelist([
        'self',
        'https://app.seoulspa.vn/**'
    ]);
});
// Hide loading when page loaded
jQuery(document).ready(function() {
    hide_loader();
    jQuery('.main-content-wrapper').css('opacity', 1);
});
/**
 * ANGULARJS SOURCE FILE
 */

// Autoload JS file
var crSegmentCf = 'dashboard';
if (currentSegment.length == 3 || currentSegment.length == 4) {
    crSegmentCf = currentSegment[1] + '/' + currentSegment[2];
} else if (currentSegment.length == 2) {
    crSegmentCf = currentSegment[1] + '/index';
}
var scriptEl = document.createElement('script'),
    scriptFile = crSegmentCf + '.js'

scriptEl.src = '/assets/js/pages/' + scriptFile + '?version=' + (new Date().getTime());
jQuery('body').append(scriptEl);


// Set option for toastr
// toastr.options = AdminBSB.toastr;

// Set cookie for referer
setCookie('referer', location.href);